spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dosopt?useSSL=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: dosopt3
    password: dosopt

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true